# Session Scratchpad
# Append-only during session. Process into long-term memory at end.

---


## Session Start — 88fdd53d
- janbam greeting + issue report: quick_start.py tool_use IDs error
- Onboarding: read memories, long-term.md, handoff.md, directory structure
- Previous session (f5b47d84) was exploration; this session is debugging

## Investigation: tool_use IDs must be unique
- Error: `messages.1.content.1: tool_use ids must be unique` — API 400 error
- Happens in with_tools_example() — uses Read/Write tools
- Simple queries (no tools) work fine
- Created debug_quick_start.py to capture stderr
- stderr only shows PERSIST log — actual error on stdout from CLI
- CLI wrapper checks CLAUDE_CODE_ENTRYPOINT — SDK sets "sdk-py" → runs vanilla
- Ran 3 times: consistently fails with tools
- Root cause: CLI bug, not SDK bug
- CLI version: 2.0.76 (local install)
- Latest CLI: 2.1.3
- Solution: likely update CLI

## Session Start — f5b47d84
- janbam here, fresh session
- Onboarding complete: absorbed handoff.md, long-term.md
- Previous session was pre-flight — this is first real exploration session
- Everything verified working: pyenv, SDK v0.1.18, 129 tests passing

## SDK → CLI Investigation
- SDK CLI discovery: bundled → shutil.which("claude") → hardcoded fallbacks
- Editable install = no bundled CLI (empty _bundled/)
- Currently using janbam's patched CLI at ~/.claude/local/claude v2.0.76
- SDK would bundle v2.0.74 if built as wheel
- MINIMUM_CLAUDE_CODE_VERSION = "2.0.0" — janbam's version is fine
- Can explicitly override: ClaudeAgentOptions(cli_path='/path/to/claude')
- SDK doesn't modify the CLI — it just spawns it as subprocess

## Created SDK_INSIGHTS.md
- docs/SDK_INSIGHTS.md — human-readable cheat sheet for non-obvious learnings
- Established in CLAUDE_LOCAL.md under "SDK Insights Cheat Sheet" section
- First entry: CLI Discovery mechanism
- Separate from memory system — for janbam to reference directly